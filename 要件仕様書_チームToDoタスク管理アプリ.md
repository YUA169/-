# チームToDoタスク管理アプリ 要件仕様書

**バージョン**: 1.1  
**作成日**: 2026-06-09  
**ステータス**: ドラフト  
**更新内容**: Googleアカウント認証 / Googleスプレッドシート DBへの変更（v1.1）

---

## 1. プロジェクト概要

### 1.1 目的

チーム内の業務効率化・可視化・コミュニケーション促進を目的としたWebベースのToDoタスク管理アプリケーションを構築する。

### 1.2 対象ユーザー

- チームメンバー（一般ユーザー）
- チームリーダー / マネージャー
- システム管理者（権限者）

### 1.3 解決する課題

| 課題 | 本システムでの解決策 |
|------|-----------------|
| タスクの属人化・進捗不透明 | 全メンバーのタスクを一元管理・可視化 |
| 業務負荷の偏り | 分析機能で業務割合を数値化 |
| タスク起点のコミュニケーション分散 | タスクに紐づいた相談・確認依頼機能 |
| 口頭確認の記録が残らない | タスク内チャットで経緯をすべて記録 |
| 別途DBサーバー費用 | Googleスプレッドシートをデータストアとして活用しコストを削減 |

---

## 2. システム構成概要

```
┌─────────────────────────────────────────────────────────┐
│                     フロントエンド                         │
│           React / TypeScript / Tailwind CSS               │
└─────────────────────────┬───────────────────────────────┘
                          │ REST API / WebSocket
┌─────────────────────────▼───────────────────────────────┐
│                      バックエンド                          │
│               Node.js (Express) / TypeScript              │
└──────────┬──────────────────────────┬───────────────────┘
           │                          │
┌──────────▼──────────────┐ ┌────────▼──────────────────┐
│  Google Workspace        │ │    リアルタイム通信         │
│  ┌────────────────────┐  │ │    Socket.IO              │
│  │ Google OAuth 2.0   │  │ └───────────────────────────┘
│  │（ユーザー認証）     │  │
│  ├────────────────────┤  │
│  │ Google Sheets API  │  │
│  │（データベース）     │  │
│  ├────────────────────┤  │
│  │ Google Drive API   │  │
│  │（ファイル添付）     │  │
│  └────────────────────┘  │
└──────────────────────────┘
```

### 2.1 設計方針

- **認証**: GoogleアカウントによるOAuth 2.0 SSOに一本化。パスワード管理不要。
- **データストア**: 権限者（Admin）が管理するGoogleスプレッドシートを唯一のデータベースとして使用。
- **アクセス制御の起点**: スプレッドシートの共有設定が最終的な権限の根拠となる。

---

## 3. 機能要件

### 3.1 ユーザー管理機能

#### 3.1.1 認証方式 — Google OAuth 2.0

| ID | 機能 | 詳細 |
|----|------|------|
| UA-01 | Googleログイン | 「Googleでログイン」ボタンから OAuth 2.0 フローを起動。初回ログイン時にユーザー情報をスプレッドシートに自動登録 |
| UA-02 | ログアウト | セッションクリア + Google OAuthセッション失効 |
| UA-03 | アクセス制限 | 権限者がスプレッドシートの「ユーザー管理シート」で許可したGoogleアカウントのみログイン可能。未登録アカウントはアクセス拒否 |
| UA-04 | プロフィール | 表示名・アバターはGoogleアカウント情報を自動取得。通知設定のみアプリ内で変更可 |

**ログインフロー**

```
①「Googleでログイン」ボタンをクリック
    ↓
②Google認証画面でアカウント選択・許可
    ↓
③バックエンドがGoogleプロフィール情報（email, name, picture）を取得
    ↓
④スプレッドシート「ユーザー管理」シートに該当メールアドレスが存在するか確認
    ↓
⑤存在する → ロール・ステータスを読み込みセッション発行
  存在しない → 「アクセス権限がありません。管理者に連絡してください。」を表示
```

#### 3.1.2 ユーザー管理（権限者操作）

権限者はスプレッドシートの「ユーザー管理」シートを直接編集してユーザーを管理する。

| 列名 | 内容 | 例 |
|------|------|----|
| email | GoogleアカウントのEmailアドレス | yamada@example.com |
| name | 表示名（空白時はGoogleアカウント名を使用） | 山田 太郎 |
| role | ロール（Admin / Manager / Member） | Member |
| team | 所属チーム名 | 開発チーム |
| status | active / inactive（inactiveでアクセス拒否） | active |
| created_at | 登録日時（自動記録） | 2026-06-09 |

#### 3.1.3 ロール定義

| ロール | 権限 |
|--------|------|
| Admin（権限者） | 全機能 + ユーザー管理シート編集 + スプレッドシート設定変更 |
| Manager | 全メンバーのタスク閲覧・編集 + 分析ダッシュボード閲覧 |
| Member | 自分および担当タスクの操作 + 相談・確認依頼送信 |

---

### 3.2 Googleスプレッドシート DB設計

#### 3.2.1 スプレッドシート構成

権限者が1つのGoogleスプレッドシートを作成し、バックエンドのサービスアカウントに編集権限を付与する。シートは以下の構成とする。

| シート名 | 役割 |
|---------|------|
| `users` | ユーザー管理（3.1.2参照） |
| `tasks` | タスクデータ |
| `chat_messages` | チャットメッセージ |
| `message_reactions` | チャットリアクション |
| `message_reads` | チャット既読管理 |
| `notifications` | 通知データ |
| `categories` | タスクカテゴリ定義 |
| `tags` | タグ定義 |
| `audit_logs` | 操作履歴 |

#### 3.2.2 各シートの列定義

**tasks シート**

| 列 | 型 | 説明 |
|----|-----|------|
| id | UUID | 主キー（自動採番） |
| title | String | タスクタイトル |
| description | String | 詳細説明 |
| type | Enum | task / consultation / review |
| status | Enum | pending / in_progress / reviewing / on_hold / done |
| priority | Enum | urgent / high / medium / low |
| category | String | カテゴリ名 |
| tags | String | カンマ区切りタグ |
| assignee_email | String | 担当者メールアドレス |
| creator_email | String | 作成者メールアドレス |
| team | String | 所属チーム |
| parent_task_id | UUID | 相談・確認タスクの元タスクID |
| due_date | Date | 期限 |
| started_at | DateTime | 着手日時 |
| completed_at | DateTime | 完了日時 |
| estimated_hours | Number | 工数見積もり（時間） |
| is_deleted | Boolean | 論理削除フラグ |
| created_at | DateTime | 作成日時 |
| updated_at | DateTime | 更新日時 |

**chat_messages シート**

| 列 | 型 | 説明 |
|----|-----|------|
| id | UUID | 主キー |
| task_id | UUID | 紐づくタスクID |
| sender_email | String | 送信者メールアドレス |
| content | String | メッセージ本文 |
| reply_to_id | UUID | 引用返信先メッセージID |
| file_drive_url | String | 添付ファイルのGoogle DriveURL |
| is_deleted | Boolean | 削除フラグ |
| created_at | DateTime | 送信日時 |
| edited_at | DateTime | 編集日時 |

#### 3.2.3 アクセス・パフォーマンス設計

Googleスプレッドシートは本来DBではないため、以下の制約と対策を明記する。

| 制約 | 対策 |
|------|------|
| API呼び出し上限（100req/100sec/user） | バックエンドでリクエストをキューイングしてレート制限を回避 |
| 読み込み速度（大量行で遅延） | 起動時にシート全体をメモリキャッシュ（Node.jsプロセス）に読み込み、変更時のみ書き込み |
| 同時書き込みの競合 | 書き込みは逐次処理（キュー方式）。楽観的ロックでコンフリクト検出 |
| 行数上限（1シート最大1000万セル） | タスク・メッセージが5万行を超えたら年度別シートに自動アーカイブ |
| リアルタイム性 | WebSocketはNode.jsのメモリキャッシュ経由で配信。スプレッドシートへの永続化は非同期で実施 |

#### 3.2.4 バックアップ・復元

- スプレッドシートのGoogle Driveバージョン履歴を最大100バージョン保持（Google標準機能）
- 月次で別スプレッドシートに自動コピーしてアーカイブ（Apps Scriptで自動化）

---

### 3.3 タスク管理機能

#### 3.3.1 タスク基本操作

| ID | 機能 | 詳細 |
|----|------|------|
| TM-01 | タスク作成 | タイトル・説明・担当者・期限・優先度・カテゴリ・タグを設定 |
| TM-02 | タスク編集 | 全フィールドの更新（変更履歴を audit_logs シートに自動記録） |
| TM-03 | タスク削除 | 論理削除（is_deleted = true）。物理削除はAdminのみ可 |
| TM-04 | タスク複製 | 既存タスクをテンプレートとして複製 |
| TM-05 | サブタスク | parent_task_id で親子関係を管理 |
| TM-06 | タスク検索 | メモリキャッシュ内をフィルタリング（キーワード・担当者・期間・ステータス・タグ） |

#### 3.3.2 タスクステータス定義

```
未着手 → 進行中 → レビュー中 → 完了
                ↓
             保留中
```

| ステータス | 説明 |
|-----------|------|
| 未着手 | 作成直後のデフォルト状態 |
| 進行中 | 担当者が着手した状態 |
| レビュー中 | 確認依頼が送信され、確認待ちの状態 |
| 保留中 | 一時停止。理由の入力を必須とする |
| 完了 | 作業終了。完了日時を自動記録 |

#### 3.3.3 タスク優先度

`緊急` / `高` / `中`（デフォルト）/ `低`

#### 3.3.4 タスクカテゴリ・タグ

- カテゴリ: 権限者がスプレッドシートの `categories` シートで定義・管理
- タグ: メンバーが自由に付与可能（`tags` シートを参照してオートコンプリート）

#### 3.3.5 表示モード

| モード | 説明 |
|--------|------|
| リスト表示 | 一覧形式。ソート・フィルタ対応 |
| ボード表示 | カンバン形式（ステータス別カラム） |
| カレンダー表示 | 期限日ベースのカレンダー |
| マイタスク | 自分が担当または関与しているタスクのみ表示 |

---

### 3.4 コミュニケーション機能

#### 3.4.1 相談機能

**操作フロー**

```
①タスク詳細画面で「相談する」ボタンをクリック
    ↓
②相談先メンバーを選択（usersシートから取得・複数選択可）
    ↓
③相談内容の初期メッセージを入力（任意）
    ↓
④「送信」をクリック
    ↓
⑤tasksシートに「相談タスク」行を追加
⑥chat_messagesシートに初期メッセージ行を追加
⑦相手のブラウザにWebSocket通知 + notificationsシートに記録
```

**相談タスクの仕様**

| 項目 | 内容 |
|------|------|
| type | `consultation` |
| タイトル | 「【相談】{元タスクタイトル}」（自動生成・変更可） |
| parent_task_id | 元タスクのIDを自動セット |
| assignee_email | 選択した相手のメールアドレス |
| ステータス | `pending` で生成 |
| 完了条件 | 相談者または相手が「相談完了」ボタンで `done` に変更 |

#### 3.4.2 確認依頼機能

**操作フロー**

```
①タスク詳細画面で「確認依頼」ボタンをクリック
    ↓
②確認者を選択（通常1名、複数選択も可）
    ↓
③確認してほしい内容・観点をメモ入力（任意）
    ↓
④「送信」をクリック
    ↓
⑤元タスクのステータスを `reviewing` に更新（tasksシート）
⑥tasksシートに「確認タスク」行を追加
⑦chat_messagesシートにメモを追加
⑧確認者にWebSocket通知 + notificationsシートに記録
```

**確認タスクの仕様**

| 項目 | 内容 |
|------|------|
| type | `review` |
| タイトル | 「【確認依頼】{元タスクタイトル}」（自動生成・変更可） |
| parent_task_id | 元タスクのIDを自動セット |
| 確認結果 | `承認` / `差し戻し` ボタンを確認者に表示 |
| 差し戻し時 | 元タスクを `in_progress` に戻す + 差し戻しコメント必須 |
| 承認時 | 確認タスクを `done` に。元タスクを `done` に移行するか選択肢を提示 |

#### 3.4.3 タスク内チャット機能

すべてのタスク（通常・相談・確認）にチャット欄を設ける。メッセージはリアルタイム配信し、`chat_messages` シートに永続化する。

| ID | 機能 | 詳細 |
|----|------|------|
| CH-01 | テキストメッセージ送信 | WebSocket経由でリアルタイム配信。非同期でスプレッドシートに書き込み |
| CH-02 | ファイル添付 | Google Driveにアップロード（上限25MB）。DriveURLをシートに保存 |
| CH-03 | メンション | `@メンバー名` で通知 |
| CH-04 | リアクション | 絵文字リアクション（`message_reactions` シートに記録） |
| CH-05 | メッセージ編集 | 送信後15分以内に限り編集可（`edited_at` を記録） |
| CH-06 | メッセージ削除 | 送信者本人のみ（`is_deleted = true`。内容は削除済み表示） |
| CH-07 | 既読表示 | `message_reads` シートで管理。参加者のアイコンで表示 |
| CH-08 | 引用返信 | `reply_to_id` で対象メッセージを参照 |
| CH-09 | 検索 | メモリキャッシュ内のメッセージを全文検索 |

---

### 3.5 通知機能

通知データは `notifications` シートに記録。ブラウザへの即時配信はWebSocketを使用。

| トリガー | 通知先 | 通知手段 |
|---------|--------|---------|
| タスクを自分に割り当てられた | 担当者 | アプリ内 + Gmailメール |
| 相談タスクが届いた | 相談受信者 | アプリ内 + Gmailメール |
| 確認依頼タスクが届いた | 確認者 | アプリ内 + Gmailメール |
| 確認が承認・差し戻された | 依頼者 | アプリ内 + Gmailメール |
| タスクの期限が翌日に迫った | 担当者 | アプリ内 + Gmailメール |
| タスクの期限を過ぎた | 担当者 + Manager | アプリ内 + Gmailメール |
| チャットでメンションされた | メンション対象者 | アプリ内 + Gmailメール |
| 参加チャットに新着メッセージ | 参加者全員 | アプリ内のみ（設定で変更可） |

> **注**: メール通知はGmail API（OAuth同一スコープ内）またはNodemailer + Googleアカウントで送信する。

---

### 3.6 分析機能

分析はスプレッドシートのキャッシュデータを集計して表示。定期的にスプレッドシートと同期する。

#### 3.6.1 個人分析ダッシュボード

| 指標 | 表示形式 | 集計単位 |
|------|---------|---------|
| タスク完了数 | 棒グラフ | 日別・週別・月別 |
| カテゴリ別業務割合 | 円グラフ / ドーナツグラフ | 週別・月別 |
| 平均タスク完了リードタイム | 数値 + 折れ線グラフ | 月別推移 |
| 優先度別タスク数 | 積み上げ棒グラフ | 週別・月別 |
| 期限遵守率 | ゲージグラフ + 数値（%） | 月別 |
| 相談・確認依頼の送受信数 | 数値 | 月別 |
| 進行中タスク一覧 | テーブル | リアルタイム |

#### 3.6.2 チーム分析ダッシュボード（Manager / Admin）

| 指標 | 表示形式 | 集計単位 |
|------|---------|---------|
| メンバー別タスク完了数比較 | 横棒グラフ | 週別・月別 |
| メンバー別カテゴリ業務割合 | 積み上げ棒グラフ | 月別 |
| チーム全体の進捗サマリ | KPIカード（完了/進行中/未着手/遅延） | リアルタイム |
| メンバー別負荷状況 | ヒートマップ（担当タスク数×期限） | 週次 |
| タスク滞留ランキング | テーブル（着手から経過日数順） | リアルタイム |
| 相談・確認依頼の流量 | ネットワーク図（誰から誰へ） | 月別 |
| 期限超過タスク一覧 | テーブル（アラート表示） | リアルタイム |

#### 3.6.3 分析データのエクスポート

- 対象期間・メンバー・カテゴリを指定してCSVエクスポート
- 権限者はスプレッドシート自体をそのままExcel / CSVとしてDLすることも可能

#### 3.6.4 業務割合の算出ロジック

```
カテゴリ別業務割合(%) =
    対象期間内に完了したカテゴリXのタスク数
    ─────────────────────────────────── × 100
    対象期間内に完了した全タスク数

※ 重み付け版（オプション）:
    estimated_hours フィールドを使い、タスク数ではなく工数合計で割合を算出
```

---

## 4. 非機能要件

### 4.1 パフォーマンス

| 項目 | 目標値 | 備考 |
|------|--------|------|
| 画面初期表示 | 2秒以内（P95） | キャッシュ利用 |
| API応答時間 | 500ms以内（P95） | スプレッドシートへの書き込みは非同期 |
| チャットメッセージ配信遅延 | 500ms以内 | WebSocket + メモリキャッシュ |
| キャッシュ同期間隔 | 30秒以内 | スプレッドシートとの差分同期 |
| 同時接続ユーザー数 | 最大50名（初期推奨） | スプレッドシートAPI制限に起因 |

### 4.2 可用性

- バックエンドサーバー稼働率: 99%以上
- Googleスプレッドシートの可用性はGoogleのSLAに依存（99.9%）
- サーバー障害時でもGoogleスプレッドシートを直接参照することでデータ確認が可能

### 4.3 セキュリティ

| 項目 | 対策 |
|------|------|
| 認証 | Google OAuth 2.0 + セッションCookie（HttpOnly / Secure） |
| アクセス制御 | usersシートのrole・statusによるRBAC |
| スプレッドシートアクセス | サービスアカウントのキーをサーバー環境変数で管理。クライアントには非公開 |
| 通信暗号化 | HTTPS (TLS 1.2以上) / WSS |
| XSS対策 | 入力値サニタイズ・CSP設定 |
| CSRF対策 | Stateパラメータ（OAuth標準）+ CSRFトークン |
| ファイルアップロード | Google DriveのMIMEタイプ検証に委任 |
| 操作ログ | audit_logsシートに重要操作を記録（90日相当分を保持） |

### 4.4 ユーザビリティ

- レスポンシブデザイン（PC / タブレット / スマートフォン対応）
- ダークモード対応
- ブラウザ対応: Chrome / Firefox / Safari / Edge（最新2バージョン）

---

## 5. データフロー

```
ブラウザ
  │  ① ページ初期表示リクエスト
  ▼
Node.jsバックエンド（メモリキャッシュ）
  │  ② キャッシュが古い場合のみスプレッドシートから再取得
  ▼
Google Sheets API
  │  ③ 全シートを一括読み込み → メモリに保存
  ▼
Node.jsバックエンド
  │  ④ キャッシュデータをJSONで返却
  ▼
ブラウザ（表示）

---書き込み時---

ブラウザ
  │  ① 書き込みリクエスト（タスク作成・更新等）
  ▼
Node.jsバックエンド
  │  ② メモリキャッシュを即時更新
  │  ③ WebSocketで他ユーザーに変更をブロードキャスト
  │  ④（非同期）書き込みキューに追加
  ▼
Google Sheets API（非同期・キュー処理）
  └  ⑤ スプレッドシートに書き込み
```

---

## 6. 画面一覧

| 画面名 | URL | アクセス権限 |
|--------|-----|------------|
| ログイン（Googleログイン） | `/login` | 全員 |
| ダッシュボード（マイページ） | `/dashboard` | Member以上 |
| タスク一覧（リスト） | `/tasks` | Member以上 |
| タスク一覧（ボード） | `/tasks/board` | Member以上 |
| タスク一覧（カレンダー） | `/tasks/calendar` | Member以上 |
| タスク詳細 + チャット | `/tasks/:id` | Member以上（関係者） |
| タスク作成 | `/tasks/new` | Member以上 |
| 通知一覧 | `/notifications` | Member以上 |
| 個人分析 | `/analytics/me` | Member以上 |
| チーム分析 | `/analytics/team` | Manager以上 |
| チーム設定 | `/settings/team` | Admin |
| ユーザー管理 | `/settings/users` | Admin（スプレッドシートへの誘導リンク） |

---

## 7. 開発フェーズ計画

### Phase 1（MVP）— 約6週間

- [ ] Google OAuth 2.0 認証
- [ ] Googleスプレッドシート接続・キャッシュ基盤
- [ ] タスクCRUD（リスト表示・ボード表示）
- [ ] タスク内チャット（テキストのみ・WebSocket）
- [ ] 相談機能・確認依頼機能（基本フロー）
- [ ] アプリ内通知

### Phase 2 — 約4週間

- [ ] 分析機能（個人・チームダッシュボード）
- [ ] カレンダー表示
- [ ] Google Driveファイル添付・メンション・リアクション
- [ ] Gmailメール通知

### Phase 3 — 約3週間

- [ ] CSVエクスポート
- [ ] スプレッドシート自動アーカイブ（Apps Script）
- [ ] モバイルレスポンシブ最適化
- [ ] パフォーマンス最適化・ダークモード

---

## 8. 技術スタック（推奨）

| レイヤー | 技術 |
|---------|------|
| フロントエンド | React 18 + TypeScript + Vite |
| UIライブラリ | Tailwind CSS + shadcn/ui |
| 状態管理 | Zustand + React Query |
| バックエンド | Node.js + Express + TypeScript |
| リアルタイム通信 | Socket.IO |
| 認証 | Google OAuth 2.0（Passport.js google-oauth20戦略） |
| データストア | Google Sheets API v4 |
| ファイル添付 | Google Drive API v3 |
| メール通知 | Gmail API / Nodemailer |
| ホスティング | Vercel（フロント） + Railway / Render（バック） |
| CI/CD | GitHub Actions |

---

## 9. Google Workspace セットアップ手順（権限者作業）

1. **スプレッドシートの作成**: 管理者のGoogleドライブに新規スプレッドシートを作成し、各シートを追加
2. **GCPプロジェクト作成**: Google Cloud Consoleでプロジェクトを作成
3. **APIの有効化**: Google Sheets API / Google Drive API / Gmail API を有効化
4. **OAuth 2.0クライアントID作成**: 承認済みリダイレクトURIにアプリのURLを登録
5. **サービスアカウント作成**: バックエンドからスプレッドシートへのアクセス用。JSONキーをダウンロード
6. **スプレッドシート共有**: サービスアカウントのメールアドレスに「編集者」権限を付与
7. **環境変数設定**: バックエンドに `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `SPREADSHEET_ID`, `SERVICE_ACCOUNT_KEY` 等を設定

---

## 10. 制約・注意事項

| 項目 | 内容 |
|------|------|
| 同時接続推奨上限 | 50名（Google Sheets APIのレート制限による） |
| スプレッドシート行数上限 | 1シートあたり約100万行（実運用では5万行でアーカイブを推奨） |
| オフライン対応 | 非対応。Googleサービスへの接続が必須 |
| スプレッドシートの直接編集 | 権限者は直接編集可能だが、キャッシュとの不整合を防ぐため通常はアプリ経由を推奨 |
| データ所在 | Googleのデータセンター（日本リージョン選択可） |

---

## 11. 用語定義

| 用語 | 定義 |
|------|------|
| タスク | 業務上の作業単位。通常タスク・相談タスク・確認タスクの3種類がある |
| 相談タスク | 「相談する」ボタンから生成される、相談のやり取りを行うタスク |
| 確認タスク | 「確認依頼」ボタンから生成される、レビュー・承認を行うタスク |
| 関連タスク | 相談タスク・確認タスクが参照する、起点となった元のタスク |
| 権限者 | Adminロールを持つユーザー。スプレッドシートおよびGCPプロジェクトの管理者 |
| サービスアカウント | バックエンドがGoogleスプレッドシートにアクセスするためのGCPサービスアカウント |
| カテゴリ | タスクの業務種別分類（権限者がcategoriesシートで定義） |
| タグ | タスクに自由付与できるラベル |
| リードタイム | タスク作成日から完了日までの経過日数 |
| メモリキャッシュ | Node.jsプロセス内にスプレッドシートデータを保持するインメモリストア |

---

*本仕様書はドラフト版です。開発着手前に関係者レビューを実施し、内容を確定してください。*
